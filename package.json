{
  "name": "english-reading-mastery",
  "version": "0.2.0",
  "private": true,
  "description": "English Mastery — a structured English reading course. Pure static HTML/CSS/JS, generated from Markdown lesson files by a dependency-light Node build script.",
  "scripts": {
    "build": "node scripts/validate-content.js && node scripts/build.js",
    "dev": "node scripts/dev.js",
    "serve": "node scripts/serve.js",
    "validate:content": "node scripts/validate-content.js",
    "new:lesson": "node scripts/new-lesson.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  }
}
