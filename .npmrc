# Drive is exFAT (no symlink support) — node_modules/.bin entries silently
# fail to resolve, and some deps (e.g. marked) ship a CLI bin npm tries to
# symlink on install. Every script in this repo is run as `node
# scripts/whatever.js` directly, never through node_modules/.bin.
bin-links=false
