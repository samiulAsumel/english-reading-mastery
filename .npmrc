# Drive is exFAT (no symlink support) — node_modules/.bin entries silently
# fail to resolve. Every npm script in this repo calls the JS entrypoint
# directly (node node_modules/<pkg>/bin/...) instead of relying on .bin.
bin-links=false
